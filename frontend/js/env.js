const config = {
  API_BASE: 'http://localhost:5000/api',
  MAPS_API_KEY: 'YOUR_GOOGLE_MAPS_API_KEY', // only needed for Google Maps
  // Geocoding: 'google' or 'nominatim'
  GEOCODER: 'nominatim', // default to open-source
};

export default config;
